<%= form_for(@fmalbum) do |f| %>
  <% if @fmalbum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fmalbum.errors.count, "error") %> prohibited this fmalbum from being saved:</h2>

      <ul>
      <% @fmalbum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :artist %><br />
    <%= f.text_field :artist %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
